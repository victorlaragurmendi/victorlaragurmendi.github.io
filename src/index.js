import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

///basicamente esta es la estructura del proyecto donde iran todos los componentes

ReactDOM.render(
  <React.StrictMode>
    <App/>
  </React.StrictMode>,
  document.getElementById('root')
);


